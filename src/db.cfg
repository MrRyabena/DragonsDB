# PostgreSQL connection config for DragonsDB server
# You can override this path via DB_CONFIG_PATH env var.
# Environment variables (PGJDBC_URL/PGUSER/PGPASSWORD) still have higher priority.

url=jdbc:postgresql://localhost:5432/studs
user=s502499
password=TSxp=3537
